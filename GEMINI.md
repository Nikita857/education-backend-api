Данный проект — это образовательная платформа на базе Spring Boot.
Система позволяет пользователям:

записываться на курсы

проходить обучение

отслеживать прогресс

получать сертификаты

Backend реализован на Java 21 с использованием Spring Boot и PostgreSQL.
Аутентификация выполнена на основе JWT и Spring Security.

Frontend (не входит в этот репозиторий) разворачивается через nginx.

⚙️ Key Technologies
Backend

Java 21

Spring Boot

Spring Security + JWT

Spring Data JPA

MapStruct (рекомендуется)

Lombok

Database

PostgreSQL

Build & Deployment

Maven

Docker / Docker Compose

nginx

🗣 Communication Requirements (Русскоязычная локализация)

Проект обязательно должен:

общаться с пользователем исключительно на русском языке

возвращать локализованные сообщения (успех, ошибки, валидация, уведомления)

использовать MessageSource

хранить все сообщения в messages_ru.properties

Запрещены:

хардкод английских сообщений

смешение языков в API

🧱 Architecture & Code Structure Requirements

Приложение обязано придерживаться строгой слоистой архитектуры:

controller → service → repository → database

Контроллеры (Controller Layer)

принимают запросы

вызывают сервисы

не содержат бизнес-логики

возвращают DTO

используют корректные HTTP-коды

Сервисы (Service Layer)

содержат бизнес-логику

используют транзакции (@Transactional)

обрабатывают данные и вызывают репозитории

Репозитории (Repository Layer)

только работа с базой данных

расширяют JpaRepository

не содержат логики, кроме запросов

DTO (Data Transfer Objects)

Строго разделяются:

*CreateRequest

*UpdateRequest

*Response

Сущности никогда не возвращаются наружу.

✔️ Coding Quality Requirements

Ниже приведены обязательные стандарты, сформированные на основе анализа текущего кода проекта.

1. Раздельные DTO для create / update / response

Использование одного DTO для всех целей — анти-pattern.

Требуется:

CourseCreateRequest

CourseUpdateRequest

CourseResponse

Это обеспечивает безопасность, правильную валидацию и гибкость API.

2. Строгая валидация входящих данных

Все методы контроллеров должны использовать:

@Valid


Все DTO обязаны содержать валидационные аннотации:

@NotNull

@Size

@Email

@Positive

@Pattern

и др.

Ни один метод не должен принимать невалидные данные.

3. Локализация всех сообщений

Все тексты ошибок, уведомлений и бизнес-сообщений должны быть вынесены в:

src/main/resources/messages_ru.properties


Контроллеры, сервисы, валидация — только через MessageSource.

4. Корректное использование HTTP-кодов

Обязательные правила:

Ситуация	Код
Создание	201 CREATED
Обновление	200 OK
Получение данных	200 OK
Удаление	204 NO CONTENT
Ошибки	через глобальный Exception Handler

Запрещено возвращать ResponseEntity.ok() во всех методах подряд.

5. Глобальный обработчик ошибок (Exception Handler)

Обязательно присутствие:

@ControllerAdvice

единого формата ошибок (ApiError)

локализованных сообщений

обработчиков для:

EntityNotFoundException

ValidationException

MethodArgumentNotValidException

AccessDeniedException

IllegalArgumentException

и др.

6. Логирование

Использовать:

@Slf4j


Логировать:

входящие параметры

ключевые бизнес-операции

ошибки

важные события

Уровни:

info — бизнес-события

warn — подозрительные состояния

error — ошибки

7. DTO для пагинации

Параметры пагинации должны передаваться единым объектом, а не множеством параметров.

Рекомендуется:

public record PaginationRequest(int page, int size, String sortBy, String sortDir) {}

8. Безопасная работа с Enum

При передаче Enum из query params Spring выбрасывает некрасивые ошибки.

Обязательно:

создать кастомный EnumConverter

добавить обработку ошибок в ExceptionHandler

9. Единый API Response Format

Использовать ApiResponse<T>:

{
  "success": true,
  "message": "Курс успешно создан",
  "data": { ... }
}


Ошибки — через ApiError.

Никаких разноформатных ответов.

10. Использование MapStruct

Для маппинга DTO ↔ Entity:

✔ повышает читаемость
✔ исключает ручные мапперы
✔ уменьшает ошибки

11. Отсутствие логики в контроллерах

Контроллеры должны быть тонкими.

Запрещается:

обрабатывать бизнес-логику

строить сложные объекты

работать с репозиториями

🔐 Authentication & Authorization

JWT является основным механизмом авторизации

Роли:

USER

INSTRUCTOR

ADMIN

HR

Ограничение доступа — через @PreAuthorize

Пароли — только BCrypt

🌐 API Design

RESTful

Версия API: /api/v1/...

Названия endpoint'ов — в kebab-case

Возврат данных — только DTO

Корректные HTTP-коды

🏗 Building and Running
Prerequisites

Java 21

Maven

Docker

Docker Compose

Run
docker-compose up -d


App доступно по адресу:
http://localhost:8080

Build
./mvnw clean install

📜 Coding Style

Java Code Conventions

Lombok для избавления от шаблонного кода

Конфигурации — только application.properties

JUnit 5 + Mockito

Интеграционные тесты — через Testcontainers